[phases.setup]
nixPkgs = ["nodejs_22", "python3", "pkg-config", "gcc"]

[phases.install]
cmds = ["npm install --ignore-scripts"]

[phases.build]
cmds = ["echo 'build done'"]

[start]
cmd = "sh bootstrap.sh"
